#include <iostream>
#include <vector>
using namespace std;

// swap- v2.swap(stringVector)-- swap v2 with stringVector
// push_back, pop_back, size(), at(i), empty, clear, swap
// if size is not initialized, values will add from the front
// v[i] = value will replace the value at i

// deep copy, shallow copy, objects, 3 files, pointers all very important
// shallow copy will not copy value of pointers, will copy address- double delete error prone

// only separate code into 3 files if EXPLICITLY told
// use practice code in modules, assignments